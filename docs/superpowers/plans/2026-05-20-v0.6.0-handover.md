# v0.6.0 Project Knowledge — Mid-Implementation Hand-Over

**Date:** 2026-05-20
**Branch:** `version/0.6.0` (pushed to origin)
**HEAD:** `88d0e4e` (`feat(validate_plan): accept project_knowledge field`)
**Plan:** [`docs/superpowers/plans/2026-05-19-project-knowledge-v0.6.0.md`](2026-05-19-project-knowledge-v0.6.0.md)
**Spec:** [`docs/superpowers/specs/2026-05-18-project-knowledge-design.md`](../specs/2026-05-18-project-knowledge-design.md)

Hand-off because the originating session's worktree had a GH PAT without `pull_requests:write` / `issues:write` scopes, which blocks both opening the draft PR and invoking CodeRabbit. The next session should resume in a worktree whose `gh` CLI can do both.

## What's done

Five commits on `version/0.6.0` ahead of `origin/main`:

| Commit | Task | Notes |
|---|---|---|
| `72f2857` | Task 0 — branch + changelog stub + v0.6.0 plan | Tracking issue NOT filed (PAT scope) — spec frontmatter still says `_to be filed before implementation_` |
| `aa8730d` | Task 0a — BM contract verified (v0.21.1; no `supersede_note` verb) + spec reconciled at all five drift points | Verified-contract block lives at the BOTTOM of the v0.6.0 plan file |
| `9fb70b2` | Task 1 — six finding categories + shared `validateFindingStrings` parser helper + three strict-mode invariants (`NoFreeformObject`, `AdditionalPropertiesFalse`, `CategoryEnumsAreInLockstep`) | Done by controller (subagent dispatch was 529'ing) |
| `3fd9bc7` | Task 2 — five env vars (`ANTI_TANGENT_KB_STORE`, `_PRIME_MODEL`, `_EXTRACT_MODEL`, `_PRIME_MAX_TOKENS`, `_EXTRACT_MAX_TOKENS`) + startup validation | Done by subagent — anti-tangent `validate_completion` verdict pass |
| `d8c59f7` | Task 3 — `project_knowledge` on `validate_task_spec` (cumulative payload cap + sentinel non-persistence test + struct-shape non-persistence test) | Done by subagent |
| `88d0e4e` | Task 4 — `project_knowledge` on `validate_plan` (`renderPlanReviewInputs` refactor, three goldens, cache-key separation, breaking `tooLargePlanResult` signature change) | Done by subagent |

All commits pass `go test -race ./...` locally. Branch was last pushed to origin after Task 4.

## What's left (Tasks 5–15)

Plan tasks remaining, in execution order. Each has a complete spec including ACs, files, steps, and a paste-ready commit message inside `docs/superpowers/plans/2026-05-19-project-knowledge-v0.6.0.md`.

| Task | Title | Scope |
|---|---|---|
| 5 | Prime verdict types, schema, parser | `PrimeResult` / `Pick` / `BMCommand` types; `prime_schema.json` with `bm_commands` required-but-can-be-empty; wire-only `primeWire` struct to block server-owned-field spoofing; `ParsePrime` reusing `validateFindingStrings`; extend the four strict-mode invariants' `schemas` slice with `PrimeSchema()` |
| 6 | Prime prompt template + golden | `internal/prompts/templates/prime.tmpl`; `PrimeInput`; `RenderPrime`; one golden |
| 7 | `prime_project_knowledge` handler | New stateless tool; clamp BEFORE payload check BEFORE model resolve (mirrors `ValidateCompletion`); `adaptivePrimeMaxTokens`; `kbStoreMismatchFindings` server-side guard; defensive `BMCommands = []verdict.BMCommand{}` when `KBStore=""` |
| 8 | Extract verdict types, schema, parser | `ExtractResult` / `Proposal`; `extract_schema.json`; `args_json` and `frontmatter_json` as JSON-encoded strings (NOT nested objects — OpenAI strict-mode constraint); wire-only `extractWire` + `proposalWire` (with `*string` pointers for `Body`/`BodyPatch`/`FrontmatterJSON` so missing-on-wire is distinguishable from present-but-empty); `ParseExtract` with presence-checks-before-action-conditional |
| 9 | Extract prompt template + golden | `extract.tmpl`; reviewer is the one emitting `bm_commands` per the verified contract (handler validates + post-processes, does NOT synthesize) |
| 10 | `extract_project_knowledge` handler | Per-envelope evidence accounting using both presence check AND `checkEvidenceShape`; evidence-bare = no valid evidence remains (TestEvidence-only is NOT evidence-bare even if FinalDiff/FinalFiles are malformed); `preFindings` merge after parse; `prependExtractClamp` helper |
| 11 | Five note templates + README | `examples/project-knowledge/{decision,module,feature,glossary,epic,README}.md` |
| 12 | INTEGRATION.md updates | New top-level "Project knowledge (optional)" section; dispatch-clause addition; "four tools" → "six tools" in all three call-sites |
| 13 | README.md updates | One paragraph + link + tools-list entries + env-var block + heading rename |
| 14 | `docs/team-setup/basic-memory-shared-vm.md` | Operator-facing setup doc; section 8 prescribes 60s systemd timer primary + inotify-recursive alternative + cron fallback; uses the verified BM contract from Task 0a |
| 15 | Finalize CHANGELOG | Reconcile the Task 0 stub with the shipped surface |

## Critical context the next session MUST read first

1. **The plan's top-of-file v0.5.2 reconciliation note** (lines ~4–15 of the plan). v0.5.2 brought `CategoryAttestationContradiction`, `HarnessShapeAttestation`, `FinalizeVerdict`/`FinalizePlanVerdict`, plus severity-ladder shifts (`tooLargeEnvelope` now `Critical` not `Major`; truncation finding now `Major` not `Minor`). Every implementer subagent must account for these.

2. **The plan's "Cross-cutting technical constraints" section** (near the top of the plan). Three load-bearing constraints repeated across Tasks 5–10:
   - OpenAI strict-mode schema rules (no freeform objects → use JSON-encoded strings like `args_json` / `frontmatter_json`; `additionalProperties: false` everywhere; required ≡ properties)
   - Server-owned vs reviewer-emitted fields (`SummaryBlock` / `Partial` are server-owned; parsers use private wire structs to block spoofing)
   - Shared `validateFindingStrings` helper (added in Task 1; Tasks 5/8 parsers call it instead of duplicating inline checks)

3. **The verified Basic Memory contract** at the bottom of the plan. Confirms the BM tool name set (`search_notes`, `read_note`, `write_note`, `edit_note`, `move_note`, `delete_note`), the absence of `supersede_note` (use the two-step `write_note` + `edit_note` mapping), and the recommended `stdio-via-SSH-proxy` transport. Tasks 6, 7, 9, 10, 12, 14 reference this block by name.

## How the subagent dispatch loop is working

For each remaining task:

1. Controller reads the full task text from the plan (use `Read` with offset/limit pointing at the `### Task N:` block through the next `---`).
2. Controller dispatches a single implementer subagent (`general-purpose`) with a self-contained prompt — see the dispatch shape used for Tasks 2/3/4 in the previous session's transcript, or copy the template at `/home/pgilmore/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/skills/subagent-driven-development/implementer-prompt.md`.
3. Subagent does TDD, runs `go test -race ./...`, commits but does NOT push.
4. Controller pushes after the subagent reports DONE (or DONE_WITH_CONCERNS with clearly-acceptable notes).
5. Once a meaningful batch is on origin, drop a `@coderabbitai review` comment on the PR. CodeRabbit findings get addressed in follow-up commits.

Spec-reviewer and code-quality-reviewer subagents (the two-stage review per the `subagent-driven-development` skill) were SKIPPED for Tasks 2–4 to conserve API capacity; the implementer subagent's self-review + `go test -race ./...` + anti-tangent's own `validate_completion` (called by the subagent) carried the quality gate. The next session can choose to add the two-stage review back in for the higher-risk Tasks 7 and 10 (the new handlers) if API capacity allows.

## Infrastructure constraints to be aware of

- **GH PAT scopes.** The originating worktree's PAT could clone/push but couldn't `gh pr create` or `gh issue create`. The next worktree's PAT MUST have at least `pull_requests:write` and `issues:write` (or be a fine-grained token with the same).
- **Anthropic API capacity.** Tasks 1's first two dispatch attempts got HTTP 529. Tasks 2/3/4 went through cleanly. If you hit 529, fall back to controller-as-implementer for that single task (Task 1 was done that way) rather than waiting indefinitely.
- **Pre-existing CodeScene flag (not in scope for v0.6.0).** Task 4 noted that `planPassCacheKey` was already over the 4-arg threshold (6 args pre-change, 7 after adding `projectKnowledge`). CodeScene will complain. File a separate follow-up issue to refactor `planPassCacheKey` to a struct arg; don't fix it inside this PR.

## Open items for the next session

1. **File the tracking issue** on `patiently/anti-tangent-mcp`. Title: "Implement v0.6.0 project-knowledge feature". Body template is in the earlier session's transcript (Task 0). After filing, replace the spec frontmatter placeholder.
2. **Open the draft PR** from `version/0.6.0` to `main`. Suggested title: `feat: v0.6.0 project-knowledge feature (WIP)`. Body should link to the spec, the plan, the tracking issue, and note that Tasks 5–15 remain.
3. **Invoke CodeRabbit** with `@coderabbitai review` once the PR is open. Address findings in follow-up commits.
4. **Resume execution at Task 5.** Use the dispatch shape that worked for Tasks 2–4.

## Quick health check on resume

```bash
git fetch origin
git switch version/0.6.0
git log --oneline origin/main..HEAD   # expect 6 commits (5 task commits + the Task 0a docs commit at 72f2857)
go test -race ./...                    # must pass before continuing
grep -n CategoryAttestationContradiction internal/verdict/verdict.go  # confirm v0.5.2 surface present
```

If any of those fail, the next session should diagnose before dispatching subagents.
