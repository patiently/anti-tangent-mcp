# v0.7.1 — BM scribe plugin + tightened v0.7.0 conventions + personal namespace

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v0.7.1 with three deliverables: (1) a working **BM-scribe plugin** at `plugin/bm-scribe/` with all twelve subcommands implemented as Claude Code skills, (2) tightened INTEGRATION.md so agents stop rediscovering the BM v0.21.1 three-step permalink pattern, and (3) a personal namespace (`<USERNAME>/todo/main.md` + `<USERNAME>/notes/<slug>/main.md`) inside the same BM project.

**Architecture:** The plugin lives as a sub-directory of this repo (`plugin/bm-scribe/`) and mirrors the superpowers plugin's shape — top-level `package.json` manifest, `README.md` + `CLAUDE.md` instruction files, and one directory per skill under `skills/<skill-name>/SKILL.md`. Skills are markdown instructions (with YAML frontmatter) that Claude follows verbatim when invoked; they call `basic-memory` MCP tools (`write_note`, `move_note`, `edit_note`, `read_note`, `search_notes`) — no plugin-side code or runtime. INTEGRATION.md edits land in place; the conventions doc and personal templates sit under `docs/team-setup/` and `examples/project-knowledge/personal/`.

**Tech Stack:** Markdown + JSON manifest. No Go code changes. CI rule: branch name `version/0.7.1` must match a `## [0.7.1] - YYYY-MM-DD` block in `CHANGELOG.md`. CHANGELOG bullets land inline with each task (per CLAUDE.md: "Add the entry as you write the code, not at the end").

**Backing issue:** [#33](https://github.com/patiently/anti-tangent-mcp/issues/33).

---

## File map

| File | Action | Responsibility |
|---|---|---|
| `CHANGELOG.md` | Modify | Add `## [0.7.1] - 2026-05-21` block; fill bullets per task. |
| `VERSION` | Modify | Bump `0.7.0` → `0.7.1` at release-ready time. |
| `docs/superpowers/specs/2026-05-21-bm-scribe-design.md` | Create | Design rationale doc for the plugin: subcommand catalogue, three-step contract, personal-namespace shape, open questions. |
| `plugin/bm-scribe/package.json` | Create | Plugin manifest (`name`, `version`, `description`). |
| `plugin/bm-scribe/gemini-extension.json` | Create | Gemini-compatible plugin metadata (`name`, `description`, `version`, `contextFileName`). |
| `plugin/bm-scribe/README.md` | Create | What the plugin is, install, the twelve subcommands. |
| `plugin/bm-scribe/CLAUDE.md` | Create | Instructions Claude follows when this plugin is active. |
| `plugin/bm-scribe/docs/three-step-pattern.md` | Create | The load-bearing `write_note → move_note → edit_note` contract, referenced by every creator skill. |
| `plugin/bm-scribe/skills/create-epic/SKILL.md` | Create | Project-knowledge epic creator. |
| `plugin/bm-scribe/skills/create-story/SKILL.md` | Create | Project-knowledge story creator. |
| `plugin/bm-scribe/skills/create-decision/SKILL.md` | Create | Project-knowledge decision creator with ADR auto-numbering. |
| `plugin/bm-scribe/skills/create-module/SKILL.md` | Create | Project-knowledge module creator. |
| `plugin/bm-scribe/skills/create-feature/SKILL.md` | Create | Project-knowledge feature creator. |
| `plugin/bm-scribe/skills/create-glossary/SKILL.md` | Create | Project-knowledge glossary term creator. |
| `plugin/bm-scribe/skills/add-todo/SKILL.md` | Create | Personal-namespace todo appender (creates `<USERNAME>/todo/main` on first use). |
| `plugin/bm-scribe/skills/list-todos/SKILL.md` | Create | Personal-namespace todo lister. |
| `plugin/bm-scribe/skills/tick-todo/SKILL.md` | Create | Personal-namespace todo completer (find_replace flip). |
| `plugin/bm-scribe/skills/add-note/SKILL.md` | Create | Personal-namespace note creator. |
| `plugin/bm-scribe/skills/fetch-note/SKILL.md` | Create | Personal-namespace note reader. |
| `plugin/bm-scribe/skills/list-notes/SKILL.md` | Create | Personal-namespace note lister. |
| `examples/project-knowledge/personal/README.md` | Create | Overview of the personal namespace. |
| `examples/project-knowledge/personal/todo.md` | Create | Template for `<USERNAME>/todo/main.md`. |
| `examples/project-knowledge/personal/note.md` | Create | Template for `<USERNAME>/notes/<slug>/main.md`. |
| `docs/team-setup/project-knowledge-conventions.md` | Modify | Add §9 "Personal namespace (`<USERNAME>/`)". |
| `INTEGRATION.md` | Modify | Move BM v0.21.1 subsection up, add worked example, add canonical-layout callout, replace lingering date-prefix decision example. No file-size constraint. |

---

## Plugin shape reference (superpowers parity)

The superpowers plugin at `/home/pgilmore/.claude/plugins/cache/superpowers-marketplace/superpowers/5.1.0/` is the reference. Verified shape (2026-05-21):

- Top-level `package.json` with `{name, version, type, main}` fields (superpowers also has `type: "module"` + `main: ".opencode/plugins/superpowers.js"` — we omit `main` since bm-scribe has no JS runtime).
- Optional `gemini-extension.json` for Gemini-compatible metadata (we ship this too for parity).
- `skills/<skill-name>/SKILL.md` per skill, with YAML frontmatter (`name`, `description`) and a markdown body.
- Top-level `README.md`, `CLAUDE.md`, optionally `AGENTS.md` / `GEMINI.md` (we ship README + CLAUDE for v0.7.1).

Skills are pure markdown instructions — no executable code in the plugin. When invoked via the `Skill` tool, the SKILL.md body is injected into the conversation and the calling agent follows it verbatim. All BM operations happen through the `basic-memory` MCP server's existing tools.

---

## Self-review notes (for agents executing this plan)

- **Branch convention:** the branch name MUST match the `[X.Y.Z]` heading in CHANGELOG.md — CI enforces this. See `CLAUDE.md` § "Branch & Version Conventions".
- **CHANGELOG bullets in-task:** each task that materially changes the repo appends one or more bullets to the matching `### Added` / `### Changed` subsection of `## [0.7.1]` before its commit. Do not save CHANGELOG bullets for a separate "finalize" task.
- **No file-size constraint on INTEGRATION.md:** prior releases (0.6.2, 0.7.0) referenced a 40,000-byte user-instructions threshold. v0.7.1 explicitly drops that constraint. Grow INTEGRATION.md as needed; readability trumps byte budget.
- **No new Go code:** no Go tests, no `go build`, no `goreleaser`. Verification is grep, `jq` on `package.json`, and read-back.
- **BM v0.21.1 contract:** every creator skill MUST encode the three-step `write_note → move_note → edit_note` pattern. Step 3 (rewriting the YAML `permalink:` line) is load-bearing — step 1+2 alone leaves wikilinks broken.
- **`<USERNAME>` resolution rule (global):** every personal-namespace skill resolves `<USERNAME>` from `$BM_SCRIBE_USERNAME` first, then falls back to `$USER` (on POSIX), then asks the user interactively if neither is set. The skill caches the answer for the rest of the session. The literal string `<USERNAME>` is NEVER written to BM — it MUST be substituted with the resolved handle before any `basic-memory` call. The plugin's `CLAUDE.md` carries this rule as a Hard Rule so all skills inherit it.

---

### Task 1: Bootstrap CHANGELOG stub on `version/0.7.1`

**Files:**
- Modify: `CHANGELOG.md` (insert new top entry above `## [0.7.0]`).

**Goal:** Land a placeholder CHANGELOG block so subsequent tasks can append bullets inline. Branch creation is assumed done (see "branch already exists" guard in Step 1).

**Acceptance criteria:**
- Branch `version/0.7.1` is checked out, was branched from `origin/main`, and `git log --oneline main..HEAD` returns no output at task start. If a pre-existing branch has prior commits, surface to the controller — do not silently reset.
- `CHANGELOG.md` has a new `## [0.7.1] - 2026-05-21` block directly above the existing `## [0.7.0] - 2026-05-21` block, with all six standard subsections (`Added`, `Changed`, `Fixed`, `Removed`, `Deprecated`, `Security`) present and empty.
- One commit lands the CHANGELOG change with subject `chore(release): start v0.7.1 work`.

**Non-goals:**
- Bumping `VERSION` (still `0.7.0`; bumped at release-ready time in Task 11).
- Pushing the branch to origin (handled at the workflow layer, not in this plan).

- [ ] **Step 1: Confirm branch state**

Run:

```bash
git rev-parse --abbrev-ref HEAD
```

Expected output: `version/0.7.1`. Also run `git log --oneline main..HEAD` and confirm it returns no output (branch has no commits past main yet). If on `main`, run `git pull --ff-only && git checkout -b version/0.7.1`. If branch exists with prior commits, surface to the controller before proceeding — do not silently reset.

- [ ] **Step 2: Insert the CHANGELOG stub**

Use the `Edit` tool against `CHANGELOG.md`. Find:

```markdown
## [0.7.0] - 2026-05-21
```

Replace with:

```markdown
## [0.7.1] - 2026-05-21

### Added

### Changed

### Fixed

### Removed

### Deprecated

### Security

## [0.7.0] - 2026-05-21
```

- [ ] **Step 3: Verify the new heading exists and is the new top entry**

Run:

```bash
grep -n '^## \[' CHANGELOG.md | head -3
```

Expected: line 1 is `## [0.7.1] - 2026-05-21`, line 2 is `## [0.7.0] - 2026-05-21`, line 3 is `## [0.6.2] - 2026-05-21`.

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md
git commit -m "chore(release): start v0.7.1 work"
```

---

### Task 2: BM-scribe design spec doc

**Files:**
- Create: `docs/superpowers/specs/2026-05-21-bm-scribe-design.md`
- Modify: `CHANGELOG.md` (append bullet under `## [0.7.1]` → `### Added`).

**Goal:** Land the design-rationale doc that explains why the plugin exists, what each subcommand does, and the three-step BM contract. Other tasks (the plugin's own `README.md`, `CLAUDE.md`, individual SKILL.md files) reference this doc as the authoritative design.

**Acceptance criteria:**
- File exists at `docs/superpowers/specs/2026-05-21-bm-scribe-design.md` (≥ 120 lines, ≥ 9 numbered top-level sections), following the same style as `docs/superpowers/specs/2026-05-21-project-knowledge-conventions-design.md`.
- Document covers: (§1) overview, (§2) goals, (§3) non-goals, (§4) plugin shape (sub-directory of anti-tangent-mcp; mirrors superpowers layout), (§5) full subcommand catalogue with twelve entries (six creators + six personal-namespace verbs), (§6) the three-step `write_note → move_note → edit_note` permalink-canonicalization contract with concrete BM v0.21.1 operation enums, (§7) personal-namespace shape, (§8) open questions, (§9) see-also.
- The subcommand catalogue names exactly these twelve subcommands: `create-epic`, `create-story`, `create-decision`, `create-module`, `create-feature`, `create-glossary`, `add-todo`, `list-todos`, `tick-todo`, `add-note`, `fetch-note`, `list-notes`.
- §6 enumerates the specific BM v0.21.1 `edit_note` operation enums the plugin uses: `find_replace`, `insert_before_section`, `replace_section`, `append`. Each enum's purpose is named at least once in the doc (`find_replace` is the load-bearing step-3 operation; `insert_before_section` is used by `add-todo` for subsequent appends; `replace_section` and `append` are documented as alternatives for future skills).
- CHANGELOG `## [0.7.1] → ### Added` gains a bullet.

**Non-goals:**
- Implementing the skills themselves (Tasks 4-6).

- [ ] **Step 1: Create the spec file**

Use `Write` to create `docs/superpowers/specs/2026-05-21-bm-scribe-design.md` with this content:

````markdown
# BM scribe — plugin design spec

**Status:** proposed
**Version target:** v0.7.1
**Backing issue:** [#33](https://github.com/patiently/anti-tangent-mcp/issues/33)
**Authors:** Patrick Gilmore

## 1. Overview

BM scribe is a Claude Code plugin shipped from this repo under `plugin/bm-scribe/`. It helps users (and the agents they dispatch) write Basic Memory notes that conform to the v0.7.0 project-knowledge conventions and to BM v0.21.1's slug-derivation behaviour. It is **advisory tooling on top of** Basic Memory's existing MCP server — it does not replace `basic-memory`, it wraps the create-with-correct-permalink workflow as a set of markdown skills.

Anti-tangent-mcp itself does not depend on BM scribe at runtime. The two are aligned by this spec.

## 2. Goals

- **Eliminate rediscovery of the three-step permalink pattern.** BM v0.21.1 ignores `permalink:` passed in metadata at `write_note` time and auto-derives a slug from `title`; `move_note` relocates the file but does not re-derive the permalink; an explicit `edit_note` against the YAML `permalink:` line is what actually lands the canonical permalink. BM scribe encodes this.
- **Make v0.7.0 layout the default.** Subcommand names map 1:1 to note types; folder shape is enforced (`<PROJECT>/<type>/<key>/main.md`, plural type folders, ADR-numbered decisions).
- **Add a per-user personal namespace.** Todos and personal notes scoped under `<USERNAME>/`, sharing the BM project with project-knowledge notes but never read by anti-tangent's `prime` / `extract`.

## 3. Non-goals

- Replacing Basic Memory's MCP server. BM scribe issues calls to `basic-memory` tools; it does not host its own storage.
- Hosting project-knowledge reviewer logic. That stays in anti-tangent-mcp.
- Multi-user concurrency. Personal namespace is single-user per Claude Code installation (the username comes from the host environment, not from a server).
- Plugin-side runtime code. All skills are markdown; the LLM is the runtime.

## 4. Plugin shape

The plugin lives at `plugin/bm-scribe/` inside this repo. Shape mirrors the superpowers plugin (verified against `superpowers/5.1.0` on 2026-05-21):

```text
plugin/bm-scribe/
├── package.json                 # manifest: {name, version, description}
├── gemini-extension.json        # Gemini-compatible metadata
├── README.md                    # what the plugin is + subcommand catalogue
├── CLAUDE.md                    # instructions when this plugin is active
├── docs/
│   └── three-step-pattern.md    # the load-bearing pattern, referenced by every creator skill
└── skills/
    ├── create-epic/SKILL.md
    ├── create-story/SKILL.md
    ├── create-decision/SKILL.md
    ├── create-module/SKILL.md
    ├── create-feature/SKILL.md
    ├── create-glossary/SKILL.md
    ├── add-todo/SKILL.md
    ├── list-todos/SKILL.md
    ├── tick-todo/SKILL.md
    ├── add-note/SKILL.md
    ├── fetch-note/SKILL.md
    └── list-notes/SKILL.md
```

The plugin reads two pieces of environment context (asked at first use if unset, with the answer cached locally by Claude Code):

- `BM_SCRIBE_PROJECT` — the BM project slug to write to (e.g. `monorepo`).
- `BM_SCRIBE_USERNAME` — the user's handle for the personal namespace (e.g. `pgilmore`). Defaults to `$USER` on POSIX.

Both can be overridden per invocation with explicit arguments.

## 5. Subcommand catalogue

### 5.1 Project-knowledge note creators

All six creators follow the three-step pattern from §6. They differ only in what they prompt for and how they shape the canonical permalink.

| Subcommand | Permalink shape | Prompts for |
|---|---|---|
| `create-epic <TICKET-ID>` | `<PROJECT>/epics/<TICKET-ID>/main` | title, charter (multi-line), `touches_modules`, `relates` |
| `create-story <TICKET-ID>` | `<PROJECT>/stories/<TICKET-ID>/main` | title, parent epic permalink, brief |
| `create-decision <slug>` | `<PROJECT>/decisions/<NNNN>-<slug>/main` | title, status (`proposed`/`accepted`), epic_origin (optional), context, decision body, consequences |
| `create-module <slug>` | `<PROJECT>/modules/<slug>/main` | title, owners, invariants list, conventions list |
| `create-feature <slug>` | `<PROJECT>/features/<slug>/main` | title, user-facing description, recent material changes (release-tagged) |
| `create-glossary <term>` | `<PROJECT>/glossary/<term>/main` | term, definition, examples, see-also |

**Auto-incrementing decision IDs.** `create-decision` first queries `search_notes` for existing `<PROJECT>/decisions/*` notes, parses the leading `NNNN-` prefix off each, and picks `max + 1` (zero-padded to four digits). If no decisions exist yet, it starts at `0001`.

### 5.2 Personal namespace

| Subcommand | Target permalink | Behaviour |
|---|---|---|
| `add-todo "<text>"` | `<USERNAME>/todo/main` | Append a `- [ ] <text>` bullet under the `## Active` section. Creates the note (via three-step) on first use. |
| `list-todos` | `<USERNAME>/todo/main` | Read the note and print all `- [ ]` and `- [x]` bullets with index numbers. |
| `tick-todo <n>` | `<USERNAME>/todo/main` | `edit_note` with `find_replace`: flip the n'th `- [ ] X` into `- [x] X` and append a date stamp. |
| `add-note <slug>` | `<USERNAME>/notes/<slug>/main` | Prompt for title + body. Three-step create. |
| `fetch-note <slug>` | `<USERNAME>/notes/<slug>/main` | `read_note` and print. |
| `list-notes` | `<USERNAME>/notes/*` | `search_notes` with prefix filter, print titles + permalinks. |

## 6. Three-step permalink-canonicalization contract

Field-tested against Basic Memory v0.21.1 (2026-05-21). BM auto-derives a note's stored slug from its `title` and ignores the `permalink:` value passed in frontmatter at `write_note` time. A second `move_note` call changes the file path but does **not** re-derive the permalink. A third `edit_note` against the YAML `permalink:` line is what actually makes the canonical permalink stick (and resolves wikilinks pointing at it).

Every `create-*` subcommand emits exactly these three operations in order, with the canonical permalink hard-coded:

```text
# Step 1 — create the note. BM ignores metadata.permalink and auto-derives.
write_note(
  title="<human title>",
  directory="<PROJECT>/<type>/<key>",
  note_type="<type>",
  content="<body>",
  metadata={..., permalink: "<PROJECT>/<type>/<key>/main"}  # BM v0.21.1 IGNORES
)

# Step 2 — move the file to the canonical path. The in-frontmatter permalink
# still reflects the auto-derived slug, NOT the canonical one.
move_note(
  identifier="<auto-derived-permalink-from-step-1>",
  destination_path="<PROJECT>/<type>/<key>/main.md"
)

# Step 3 — rewrite the YAML permalink line so it matches the path.
# Without this, cross-links written as [[<PROJECT>/<type>/<key>/main]] fail
# to resolve.
edit_note(
  identifier="<PROJECT>/<type>/<key>/main",
  operation="find_replace",
  find_text="permalink: <auto-derived-permalink>",
  replace_text="permalink: <PROJECT>/<type>/<key>/main"
)
```

The plugin captures step 1's actual return value to feed step 2's `identifier`, and the YAML body of the note after step 2 to feed step 3's `find_text`.

## 7. Personal-namespace shape

Personal namespace lives in the **same BM project** as project-knowledge notes, namespaced by the user's handle:

```text
<PROJECT>/
├── epics/<TICKET-ID>/main.md            # project knowledge
├── stories/<TICKET-ID>/main.md
├── decisions/<NNNN>-<slug>/main.md
├── modules/<slug>/main.md
├── features/<slug>/main.md
└── glossary/<term>/main.md

<USERNAME>/                              # personal namespace (one per Claude install)
├── todo/main.md                         # one rolling checkbox list
└── notes/<slug>/main.md                 # one note per topic
```

**Anti-tangent ignores the personal namespace.** `prime_project_knowledge` and `extract_project_knowledge` operate on the `<PROJECT>/` prefix only. The boundary is enforced at the `kb_index` filter the controller passes in, not by BM scribe — BM scribe owns the WRITE side of the namespace.

## 8. Open questions

- **Spin-out to a sibling repo.** v0.7.1 ships the plugin co-located in this repo for simpler iteration. Once the contract is stable, spin out to `patiently/bm-scribe` and reference from anti-tangent-mcp's README. Tracked separately.
- **Should `list-todos` distinguish "active" vs "done"?** Current spec treats the note as one flat list with state on each bullet, sectioned by `## Active` / `## Done`. Could fold into a single linear list.
- **Concurrent todo edits.** No locking; if two `tick-todo` calls race, the second one's `find_replace` may target a stale state. Acceptable for single-user usage; revisit if multi-pane Claude Code installs grow.

## 9. See also

- [#33](https://github.com/patiently/anti-tangent-mcp/issues/33) — backing issue.
- [v0.7.0 project-knowledge conventions](2026-05-21-project-knowledge-conventions-design.md) — the layout this plugin aligns BM-scribe with.
- [`INTEGRATION.md` § "Applying bm_commands to BM v0.21.1"](../../../INTEGRATION.md) — the BM v0.21.1 contract this plugin encodes.
- [`docs/team-setup/project-knowledge-conventions.md`](../../team-setup/project-knowledge-conventions.md) — per-team tuning loop; §9 (added in v0.7.1) covers the personal namespace.
- [`plugin/bm-scribe/`](../../../plugin/bm-scribe/) — the plugin implementation.
````

- [ ] **Step 2: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Added` under `## [0.7.1]`:

```markdown
- New design spec `docs/superpowers/specs/2026-05-21-bm-scribe-design.md` for the BM-scribe plugin: twelve subcommands across project-knowledge creators and personal-namespace verbs, the three-step `write_note → move_note → edit_note` permalink-canonicalization contract field-tested against BM v0.21.1, and the personal-namespace shape.
```

- [ ] **Step 3: Verify the spec file**

```bash
wc -l docs/superpowers/specs/2026-05-21-bm-scribe-design.md
grep -c '^## ' docs/superpowers/specs/2026-05-21-bm-scribe-design.md
```

Expected: ≥ 120 lines and ≥ 9 top-level sections.

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/specs/2026-05-21-bm-scribe-design.md CHANGELOG.md
git commit -m "docs(spec): add BM scribe design spec (v0.7.1)"
```

---

### Task 3: BM-scribe plugin scaffolding

**Files:**
- Create: `plugin/bm-scribe/package.json`
- Create: `plugin/bm-scribe/gemini-extension.json`
- Create: `plugin/bm-scribe/README.md`
- Create: `plugin/bm-scribe/CLAUDE.md`
- Create: `plugin/bm-scribe/docs/three-step-pattern.md`
- Modify: `CHANGELOG.md` (append bullet under `### Added`).

**Goal:** Stand up the plugin directory with manifest, top-level docs, and the load-bearing three-step pattern reference doc that every creator skill points back to.

**Acceptance criteria:**
- `plugin/bm-scribe/package.json` is valid JSON with `name = "bm-scribe"`, `version = "0.1.0"`, `description` field, and no `main` (no JS runtime).
- `plugin/bm-scribe/gemini-extension.json` is valid JSON mirroring superpowers' shape (`name`, `description`, `version`, `contextFileName: "CLAUDE.md"`).
- `plugin/bm-scribe/README.md` exists, ≥ 40 lines, with the twelve-subcommand table and a link to the design spec.
- `plugin/bm-scribe/CLAUDE.md` exists, instructs Claude on the plugin's posture (advisory wrapper; always emit three-step pattern for creators; never short-cut step 3). Includes a Hard Rule stating `<USERNAME>` resolves from `$BM_SCRIBE_USERNAME` then `$USER`, falling back to a user prompt; the literal string `<USERNAME>` is never written to BM (always substitute with the resolved handle).
- `plugin/bm-scribe/docs/three-step-pattern.md` exists with literal worked example.
- CHANGELOG bullet added.

**Non-goals:**
- Writing any SKILL.md files (Tasks 4-6 handle those).
- Bumping the plugin version above `0.1.0` (initial release).

- [ ] **Step 1: Create the directory layout**

```bash
mkdir -p plugin/bm-scribe/docs plugin/bm-scribe/skills
```

- [ ] **Step 2: Create `plugin/bm-scribe/package.json`**

Use `Write`:

```json
{
  "name": "bm-scribe",
  "version": "0.1.0",
  "description": "Claude Code plugin that writes Basic Memory notes per v0.7.0 project-knowledge conventions and the BM v0.21.1 three-step permalink-canonicalization pattern."
}
```

- [ ] **Step 3: Create `plugin/bm-scribe/gemini-extension.json`**

Use `Write`:

```json
{
  "name": "bm-scribe",
  "description": "Claude Code plugin that writes Basic Memory notes per v0.7.0 project-knowledge conventions and the BM v0.21.1 three-step permalink-canonicalization pattern.",
  "version": "0.1.0",
  "contextFileName": "CLAUDE.md"
}
```

- [ ] **Step 4: Create `plugin/bm-scribe/README.md`**

Use `Write`:

````markdown
# bm-scribe

A Claude Code plugin that writes Basic Memory notes per the v0.7.0 project-knowledge conventions (see [`anti-tangent-mcp`](https://github.com/patiently/anti-tangent-mcp)) and the BM v0.21.1 three-step permalink-canonicalization pattern (see [`docs/three-step-pattern.md`](docs/three-step-pattern.md)).

## What it does

Wraps the standard `basic-memory` MCP tools (`write_note`, `move_note`, `edit_note`, `read_note`, `search_notes`) with twelve narrowly-scoped skills that enforce:

- v0.7.0 canonical permalink layout: `<PROJECT>/<type>/<key>/main`, plural type folders, ADR-numbered decisions.
- The three-step pattern: `write_note` (BM auto-derives the wrong slug) → `move_note` (path moves, permalink stays auto-derived) → `edit_note` find_replace on the YAML `permalink:` line (canonical permalink finally sticks).
- A per-user personal namespace at `<USERNAME>/todo/main` (rolling checkbox list) and `<USERNAME>/notes/<slug>/main` (per-topic notes).

The plugin is markdown-only — no executable runtime. All BM operations happen through the `basic-memory` MCP server.

## Subcommands

| Skill | What it does |
|---|---|
| `create-epic <TICKET-ID>` | Create a project-knowledge epic at `<PROJECT>/epics/<TICKET-ID>/main`. |
| `create-story <TICKET-ID>` | Create a project-knowledge story at `<PROJECT>/stories/<TICKET-ID>/main`. |
| `create-decision <slug>` | Create a project-knowledge decision at `<PROJECT>/decisions/<NNNN>-<slug>/main` with auto-picked ADR number. |
| `create-module <slug>` | Create a project-knowledge module at `<PROJECT>/modules/<slug>/main`. |
| `create-feature <slug>` | Create a project-knowledge feature at `<PROJECT>/features/<slug>/main`. |
| `create-glossary <term>` | Create a project-knowledge glossary term at `<PROJECT>/glossary/<term>/main`. |
| `add-todo "<text>"` | Append a checkbox bullet to `<USERNAME>/todo/main` (creates note on first use). |
| `list-todos` | Print all checkbox bullets from `<USERNAME>/todo/main` with index numbers. |
| `tick-todo <n>` | Flip the n'th unchecked bullet to checked + date stamp. |
| `add-note <slug>` | Create a personal note at `<USERNAME>/notes/<slug>/main`. |
| `fetch-note <slug>` | Print a personal note at `<USERNAME>/notes/<slug>/main`. |
| `list-notes` | Print all personal notes under `<USERNAME>/notes/*`. |

## Environment

- `BM_SCRIBE_PROJECT` — BM project slug to write to (e.g. `monorepo`). Asked at first use if unset.
- `BM_SCRIBE_USERNAME` — handle for the personal namespace (e.g. `pgilmore`). Defaults to `$USER`.

## See also

- [Design spec](../../docs/superpowers/specs/2026-05-21-bm-scribe-design.md) — full rationale and contract.
- [Three-step pattern](docs/three-step-pattern.md) — the BM v0.21.1 quirk every creator skill encodes.
````

- [ ] **Step 5: Create `plugin/bm-scribe/CLAUDE.md`**

Use `Write`:

````markdown
# bm-scribe — instructions for Claude

When a bm-scribe skill is invoked, follow it verbatim. The skill body tells you what to ask the user, what to call on `basic-memory`, and how to verify success.

## Posture

This plugin is **advisory wrapper** over Basic Memory. It exists because BM v0.21.1 ignores the `permalink:` you pass in metadata at `write_note` time and auto-derives a slug from the title. A single `write_note` call lands the note at the wrong permalink; a `move_note` relocates the file but leaves the YAML `permalink:` field pointing at the auto-derived slug; only an explicit `edit_note` against that YAML line lands the canonical permalink. Skip step 3 and wikilinks break.

## Hard rules

- **Always emit the three-step pattern for any creator skill.** Never short-circuit step 3 even if step 1 appears to have landed the correct permalink — BM might have auto-derived a slug that *looks* right but isn't.
- **Capture step 1's returned permalink verbatim.** It is the input to step 2 (`identifier`). Do not assume it matches the metadata `permalink:` you passed.
- **Read the moved note after step 2** to extract the current YAML `permalink:` value. That's the `find_text` for step 3. Do not guess it — BM normalises slugs in ways that can surprise (case, hyphenation, embedded dates).
- **Never write outside the user's BM project.** `BM_SCRIBE_PROJECT` scopes every call; if unset, ask the user before any write.
- **Personal namespace stays personal.** `<USERNAME>/todo/main` and `<USERNAME>/notes/<slug>/main` are written by this plugin and are explicitly NOT scanned by anti-tangent's `prime` / `extract`. Do not propose adding them to a project-knowledge `kb_index`.
- **Resolve `<USERNAME>` before every personal-namespace call.** Use `$BM_SCRIBE_USERNAME` first, then `$USER` (POSIX), then ask the user interactively and cache the answer for the session. The literal string `<USERNAME>` is **never** written to Basic Memory — always substitute the resolved handle before any `basic-memory` call.

## Reference

- [`docs/three-step-pattern.md`](docs/three-step-pattern.md) — the load-bearing pattern with worked example.
- [Design spec](../../docs/superpowers/specs/2026-05-21-bm-scribe-design.md) — full rationale.
````

- [ ] **Step 6: Create `plugin/bm-scribe/docs/three-step-pattern.md`**

Use `Write`:

````markdown
# The three-step permalink-canonicalization pattern

Field-tested against Basic Memory v0.21.1 (2026-05-21). Every creator skill in bm-scribe emits exactly these three operations.

## Why three steps

BM v0.21.1 ignores the `permalink:` you pass in `write_note`'s `metadata` and auto-derives a slug from `title` (lowercased, hyphenated, normalised). `move_note` relocates the file but leaves the YAML `permalink:` value pointing at the auto-derived slug. `edit_note` against that YAML line is what finally lands the canonical permalink.

Skip step 3 and any wikilink written as `[[<canonical-permalink>]]` will fail to resolve, because BM's index keys on the YAML `permalink:` field, not on the file path.

## Worked example — creating one epic note

```text
# Step 1 — write the note. BM ignores metadata.permalink and auto-derives.
write_note(
  title="YN-10206 — DriverInvite testability",
  directory="monorepo/epics/YN-10206",
  note_type="epic",
  content="<charter body>",
  metadata={ permalink: "monorepo/epics/YN-10206/main", ... }
)
# → BM creates: main/monorepo/epics/yn-10206-driverinvite-testability.md
# → YAML permalink:  main/monorepo/epics/yn-10206-driverinvite-testability
# → NOT the canonical permalink we asked for.

# Step 2 — move the file to the canonical path.
move_note(
  identifier="main/monorepo/epics/yn-10206-driverinvite-testability",
  destination_path="monorepo/epics/YN-10206/main.md"
)
# → File now lives at: monorepo/epics/YN-10206/main.md
# → BUT the YAML permalink: line still reads:
#   permalink: main/monorepo/epics/yn-10206-driverinvite-testability
# → Wikilinks like [[monorepo/epics/YN-10206/main]] do NOT resolve yet.

# Step 3 — rewrite the YAML permalink line. Load-bearing.
edit_note(
  identifier="monorepo/epics/YN-10206/main",
  operation="find_replace",
  find_text="permalink: main/monorepo/epics/yn-10206-driverinvite-testability",
  replace_text="permalink: monorepo/epics/YN-10206/main"
)
# → Frontmatter updated. Wikilinks [[monorepo/epics/YN-10206/main]] resolve.
```

## How to find step 3's `find_text`

After step 2, read the note (`read_note` with the canonical permalink as identifier) and extract the YAML `permalink:` line verbatim — case, hyphens, embedded date prefixes and all. That string is your `find_text`. Guessing fails when BM's slug-normalisation rules surprise you.

## What to verify after step 3

- `read_note` against the canonical permalink succeeds.
- The YAML `permalink:` field matches the canonical permalink you wanted.
- Any wikilinks in the body that point to the canonical permalink resolve (BM's `read_note` output includes resolved-relation counts).
````

- [ ] **Step 7: Validate manifest JSON**

```bash
jq . plugin/bm-scribe/package.json
jq . plugin/bm-scribe/gemini-extension.json
```

Both should print parsed JSON with no errors.

- [ ] **Step 8: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Added` under `## [0.7.1]`:

```markdown
- New `plugin/bm-scribe/` Claude Code plugin scaffolding: `package.json` + `gemini-extension.json` manifests, `README.md` with the twelve-subcommand catalogue, `CLAUDE.md` instructing the plugin's posture (always emit the three-step pattern, never short-cut step 3), and `docs/three-step-pattern.md` with a literal worked example for the load-bearing `write_note → move_note → edit_note` contract.
```

- [ ] **Step 9: Commit**

```bash
git add plugin/bm-scribe/ CHANGELOG.md
git commit -m "feat(plugin): scaffold bm-scribe plugin (manifest + docs)"
```

---

### Task 4: Implement six project-knowledge creator skills

**Files:**
- Create: `plugin/bm-scribe/skills/create-epic/SKILL.md`
- Create: `plugin/bm-scribe/skills/create-story/SKILL.md`
- Create: `plugin/bm-scribe/skills/create-decision/SKILL.md`
- Create: `plugin/bm-scribe/skills/create-module/SKILL.md`
- Create: `plugin/bm-scribe/skills/create-feature/SKILL.md`
- Create: `plugin/bm-scribe/skills/create-glossary/SKILL.md`
- Modify: `CHANGELOG.md` (append bullet).

**Goal:** Land six SKILL.md files that all encode the same three-step pattern, differing only in their prompts, frontmatter (note type + type-specific scaffolding), and canonical permalink shape.

**Acceptance criteria:**
- All six SKILL.md files exist and have valid YAML frontmatter with `name` matching `bm-scribe:create-<type>` and a `description` field.
- Each SKILL.md body has four sections: "Step 1 — Gather inputs", "Step 2 — Resolve project + permalink", "Step 3 — Issue the three-step BM call sequence", "Step 4 — Verify".
- The three-step section in every creator references `docs/three-step-pattern.md` and shows the literal BM call shape parameterised for that note type.
- `create-decision` includes an explicit "Auto-pick ADR number" sub-step using `search_notes`.
- CHANGELOG bullet added.

**Non-goals:**
- Writing personal-namespace skills (Tasks 5-6).
- Hand-coding the auto-ADR logic in any language other than markdown instructions (the LLM does the math at invocation time).

- [ ] **Step 1: Read the canonical creator-skill template**

Below is the template every creator follows. Substitute `<TYPE>`, `<KEY>`, `<KEY-DESCRIPTION>`, `<PERMALINK-SHAPE>`, `<TYPE-PROMPTS>`, and `<FRONTMATTER-FIELDS>` per the parameterisation table in Step 2.

````markdown
---
name: bm-scribe:create-<TYPE>
description: Use when creating a new project-knowledge <TYPE> note. Walks through the three-step BM v0.21.1 pattern and lands the note at the canonical v0.7.0 permalink `<PERMALINK-SHAPE>`.
---

# create-<TYPE>

Creates a project-knowledge `<TYPE>` note at `<PERMALINK-SHAPE>` per the [v0.7.0 canonical layout](../../../../docs/superpowers/specs/2026-05-21-project-knowledge-conventions-design.md) and the [three-step pattern](../../docs/three-step-pattern.md).

## Step 1 — Gather inputs

Ask the user for:

- `<KEY>` (<KEY-DESCRIPTION>) — required, comes from the invocation argument if provided.
<TYPE-PROMPTS>

If `BM_SCRIBE_PROJECT` is unset, ask the user which BM project to write to and remember the answer for the rest of this session.

## Step 2 — Resolve project + permalink

- `<PROJECT>` = `$BM_SCRIBE_PROJECT` (or the answer from Step 1).
- Canonical permalink = `<PERMALINK-SHAPE>` (substitute `<PROJECT>` and `<KEY>`).
- Directory portion (passed to `write_note`) = canonical permalink with the trailing `/main` stripped.

## Step 3 — Issue the three-step BM call sequence

Follow [`docs/three-step-pattern.md`](../../docs/three-step-pattern.md). Concretely:

```text
# Step 3a — create. BM ignores metadata.permalink.
basic-memory:write_note(
  title=<title>,
  directory="<PROJECT>/<TYPE-FOLDER>/<KEY>",
  note_type="<TYPE>",
  content=<rendered body>,
  metadata={
    permalink: "<PERMALINK-SHAPE>",
<FRONTMATTER-FIELDS>
  }
)
# Capture the returned permalink — call it AUTO_PERMALINK.

# Step 3b — relocate.
basic-memory:move_note(
  identifier=AUTO_PERMALINK,
  destination_path="<PROJECT>/<TYPE-FOLDER>/<KEY>/main.md"
)

# Step 3c — read the moved note to capture the YAML permalink line verbatim.
basic-memory:read_note(identifier="<PERMALINK-SHAPE>")
# Extract the current `permalink: …` line from the frontmatter; call it CURRENT_PERMALINK_LINE.

# Step 3d — rewrite the YAML permalink line.
basic-memory:edit_note(
  identifier="<PERMALINK-SHAPE>",
  operation="find_replace",
  find_text=CURRENT_PERMALINK_LINE,
  replace_text="permalink: <PERMALINK-SHAPE>"
)
```

## Step 4 — Verify

- `basic-memory:read_note(identifier="<PERMALINK-SHAPE>")` returns the note.
- The YAML `permalink:` field in the returned frontmatter equals `<PERMALINK-SHAPE>` exactly.
- Report success to the user with the canonical permalink in a paste-ready form: `[[<PERMALINK-SHAPE>]]`.
````

- [ ] **Step 2: Parameterise per creator**

For each row below, substitute into the template from Step 1 and `Write` the resulting file to the listed path.

**create-epic** → `plugin/bm-scribe/skills/create-epic/SKILL.md`

- `<TYPE>` = `epic`
- `<TYPE-FOLDER>` = `epics`
- `<KEY>` = `TICKET-ID`
- `<KEY-DESCRIPTION>` = tracker ticket ID (e.g. `YN-10206`, `gh-23`)
- `<PERMALINK-SHAPE>` = `<PROJECT>/epics/<TICKET-ID>/main`
- `<TYPE-PROMPTS>` (additional bullets under Step 1):

  ```
  - `title` — one-line human title.
  - `charter` — multi-line description of the epic's goal and scope.
  - `touches_modules` — list of module permalinks this epic touches (optional).
  - `relates` — list of related permalinks (optional).
  ```

- `<FRONTMATTER-FIELDS>` (lines under metadata in step 3a):

  ```
    status: "open",
    touches_modules: <list>,
    relates: <list>,
  ```

**create-story** → `plugin/bm-scribe/skills/create-story/SKILL.md`

- `<TYPE>` = `story`
- `<TYPE-FOLDER>` = `stories`
- `<KEY>` = `TICKET-ID`
- `<KEY-DESCRIPTION>` = tracker ticket ID for the story
- `<PERMALINK-SHAPE>` = `<PROJECT>/stories/<TICKET-ID>/main`
- `<TYPE-PROMPTS>`:

  ```
  - `title` — one-line human title.
  - `parent_epic` — permalink of the parent epic (e.g. `<PROJECT>/epics/YN-10206/main`).
  - `brief` — multi-line story brief.
  ```

- `<FRONTMATTER-FIELDS>`:

  ```
    status: "open",
    parent_epic: <parent_epic>,
  ```

**create-decision** → `plugin/bm-scribe/skills/create-decision/SKILL.md`

- `<TYPE>` = `decision`
- `<TYPE-FOLDER>` = `decisions`
- `<KEY>` = `NNNN-<slug>` (auto-picked NNNN + user-provided slug)
- `<KEY-DESCRIPTION>` = a kebab-case slug for the decision
- `<PERMALINK-SHAPE>` = `<PROJECT>/decisions/<NNNN>-<slug>/main`
- `<TYPE-PROMPTS>`:

  ```
  - `slug` — kebab-case description (e.g. `text-only-reviewer`).
  - `title` — one-line human title for the decision.
  - `status` — `proposed` (default) or `accepted`.
  - `epic_origin` — optional permalink of the epic that triggered this decision.
  - `context` — what's true that makes this decision necessary.
  - `decision` — the decision itself.
  - `consequences` — what this decision implies (good and bad).
  ```

  Then add a sub-step inside Step 2 (after the canonical-permalink resolution, since the permalink depends on `NNNN`): **Auto-pick ADR number.** Query `basic-memory:search_notes` with prefix `<PROJECT>/decisions/`. Parse the leading `NNNN-` off each returned permalink, find the max, and set `NNNN = max + 1` (zero-padded to four digits). If no decisions exist, start at `0001`. The final canonical permalink for this skill is `<PROJECT>/decisions/<NNNN>-<slug>/main` with `NNNN` resolved at this point.

- `<FRONTMATTER-FIELDS>`:

  ```
    status: <status>,
    epic_origin: <epic_origin or null>,
  ```

**create-module** → `plugin/bm-scribe/skills/create-module/SKILL.md`

- `<TYPE>` = `module`
- `<TYPE-FOLDER>` = `modules`
- `<KEY>` = `<slug>`
- `<KEY-DESCRIPTION>` = a kebab-case module name
- `<PERMALINK-SHAPE>` = `<PROJECT>/modules/<slug>/main`
- `<TYPE-PROMPTS>`:

  ```
  - `title` — one-line human title.
  - `owners` — list of owner handles.
  - `invariants` — list of invariants this module enforces.
  - `conventions` — list of in-module conventions.
  ```

- `<FRONTMATTER-FIELDS>`:

  ```
    owners: <list>,
  ```

**create-feature** → `plugin/bm-scribe/skills/create-feature/SKILL.md`

- `<TYPE>` = `feature`
- `<TYPE-FOLDER>` = `features`
- `<KEY>` = `<slug>`
- `<KEY-DESCRIPTION>` = a kebab-case feature name
- `<PERMALINK-SHAPE>` = `<PROJECT>/features/<slug>/main`
- `<TYPE-PROMPTS>`:

  ```
  - `title` — one-line human title.
  - `description` — user-facing description.
  - `recent_changes` — list of release-tagged change pointers (e.g. `v0.6.0 — added X`).
  ```

- `<FRONTMATTER-FIELDS>`: (no per-type frontmatter beyond the `permalink` carried by the template; `note_type` comes from the top-level `note_type` kwarg, not metadata)

**create-glossary** → `plugin/bm-scribe/skills/create-glossary/SKILL.md`

- `<TYPE>` = `glossary`
- `<TYPE-FOLDER>` = `glossary`
- `<KEY>` = `<term>`
- `<KEY-DESCRIPTION>` = the term being defined, in kebab-case
- `<PERMALINK-SHAPE>` = `<PROJECT>/glossary/<term>/main`
- `<TYPE-PROMPTS>`:

  ```
  - `term` — the canonical term.
  - `definition` — one-paragraph definition.
  - `examples` — list of usage examples.
  - `see_also` — list of related permalinks.
  ```

- `<FRONTMATTER-FIELDS>`: (no per-type frontmatter beyond the `permalink` carried by the template; `note_type` comes from the top-level `note_type` kwarg, not metadata)

- [ ] **Step 3: Verify all six SKILL.md files exist**

```bash
ls plugin/bm-scribe/skills/create-*/SKILL.md
```

Expected: six paths printed (one per creator).

- [ ] **Step 4: Verify frontmatter parses for each**

```bash
for f in plugin/bm-scribe/skills/create-*/SKILL.md; do
  head -5 "$f" | grep -E '^name:|^description:' | wc -l
done
```

Expected: six lines, each printing `2` (one `name:` + one `description:` per file).

- [ ] **Step 5: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Added` under `## [0.7.1]`:

```markdown
- Six project-knowledge creator skills under `plugin/bm-scribe/skills/`: `create-epic`, `create-story`, `create-decision` (with `search_notes`-based ADR auto-numbering), `create-module`, `create-feature`, `create-glossary`. All six encode the three-step `write_note → move_note → edit_note` BM v0.21.1 pattern and land at canonical v0.7.0 permalinks (`<PROJECT>/<type-plural>/<key>/main`).
```

- [ ] **Step 6: Commit**

```bash
git add plugin/bm-scribe/skills/create-* CHANGELOG.md
git commit -m "feat(plugin): add six project-knowledge creator skills"
```

---

### Task 5: Implement three personal-namespace todo skills

**Files:**
- Create: `plugin/bm-scribe/skills/add-todo/SKILL.md`
- Create: `plugin/bm-scribe/skills/list-todos/SKILL.md`
- Create: `plugin/bm-scribe/skills/tick-todo/SKILL.md`
- Modify: `CHANGELOG.md` (append bullet).

**Goal:** Three skills that own the personal todo list at `<USERNAME>/todo/main`. `add-todo` does first-time-create-via-three-step + subsequent-edit-via-edit_note; `list-todos` reads; `tick-todo` flips a checkbox.

**Acceptance criteria:**
- All three SKILL.md files exist with valid YAML frontmatter.
- All three skills resolve `<USERNAME>` per the global rule (`$BM_SCRIBE_USERNAME` → `$USER` → ask the user) and substitute the resolved handle into every `basic-memory` call. The literal string `<USERNAME>` is never sent to BM.
- `add-todo`'s body probes for the note's existence first: it calls `basic-memory:read_note(identifier="<resolved-username>/todo/main")`; if the call returns a not-found error, take branch (a); if the call returns a note, take branch (b). The two branches are: (a) note doesn't exist yet — do the full three-step create with the todo-note body shape inlined verbatim in the skill (frontmatter: `title: <resolved-username>'s todo`, `permalink: <resolved-username>/todo/main`, `note_type: personal_todo`; body with `## Active` and `## Done` sections; the first user-supplied bullet lands under `## Active`); (b) note exists — single `edit_note` with `operation: insert_before_section` keyed on `## Done` (so new bullets land at the bottom of `## Active`).
- `list-todos` reads the note and prints each `- [ ]` / `- [x]` bullet with a 1-based numeric index over ALL bullets (both checked and unchecked). The displayed index becomes the `n` argument that `tick-todo` accepts.
- `tick-todo`'s `n` argument is the 1-based displayed index from `list-todos` (counts both checked and unchecked bullets). `tick-todo` uses `edit_note` with `operation: find_replace` against the n'th bullet's literal text; if the target bullet is already checked, the skill reports "Already done." and exits without editing.
- CHANGELOG bullet added.

**Non-goals:**
- Implementing note skills (Task 6).
- Concurrency safety (single-user assumption per the spec).

- [ ] **Step 1: Create `plugin/bm-scribe/skills/add-todo/SKILL.md`**

Use `Write`:

````markdown
---
name: bm-scribe:add-todo
description: Use when adding a personal todo to your rolling list. Appends a `- [ ] <text>` bullet to `<USERNAME>/todo/main`. Creates the note on first use.
---

# add-todo

Appends a checkbox bullet to your personal rolling todo list at `<USERNAME>/todo/main`. On first use, creates the note via the three-step pattern.

## Step 1 — Gather inputs

- `text` — the todo content. Comes from the invocation argument.
- `<USERNAME>` = `$BM_SCRIBE_USERNAME`, then `$USER` (POSIX). If both unset, ask the user for their handle and cache the answer for the session.

## Step 2 — Check whether the todo note exists

Call:

```text
basic-memory:read_note(identifier="<USERNAME>/todo/main")
```

If it returns the note, proceed to Step 3a (append). If it errors with "not found", proceed to Step 3b (create).

## Step 3a — Note exists: append a bullet

Call:

```text
basic-memory:edit_note(
  identifier="<USERNAME>/todo/main",
  operation="insert_before_section",
  section="## Done",
  content="- [ ] <text>\n"
)
```

The `insert_before_section` operation puts the new bullet at the bottom of the section above `## Done`, i.e. at the end of `## Active`.

## Step 3b — Note doesn't exist: create then append

Issue the full three-step pattern (see [`../../docs/three-step-pattern.md`](../../docs/three-step-pattern.md)). The note body is the following template, with `<text>` substituted from Step 1 and `<USERNAME>` substituted from your env. The note shape is also documented in `examples/project-knowledge/personal/todo.md` (in the anti-tangent-mcp repo), but the skill does NOT depend on that file existing at install time — the template is inlined here verbatim:

```text
---
title: <USERNAME>'s todo
permalink: <USERNAME>/todo/main
note_type: personal_todo
---

# Todo

One rolling checkbox list for personal todos. Edited via `bm-scribe:add-todo` / `bm-scribe:tick-todo` / `bm-scribe:list-todos`, or directly in any markdown editor.

## Active

- [ ] <text>

## Done
```

```text
# 3b.i — write the note.
basic-memory:write_note(
  title="<USERNAME>'s todo",
  directory="<USERNAME>/todo",
  note_type="personal_todo",
  content=<rendered template with - [ ] <text> as the first bullet>,
  metadata={ permalink: "<USERNAME>/todo/main" }
)
# Capture AUTO_PERMALINK from the return value.

# 3b.ii — move.
basic-memory:move_note(
  identifier=AUTO_PERMALINK,
  destination_path="<USERNAME>/todo/main.md"
)

# 3b.iii — read to get the current YAML permalink line.
basic-memory:read_note(identifier="<USERNAME>/todo/main")
# Extract the `permalink: …` line.

# 3b.iv — rewrite the permalink.
basic-memory:edit_note(
  identifier="<USERNAME>/todo/main",
  operation="find_replace",
  find_text=<current permalink line>,
  replace_text="permalink: <USERNAME>/todo/main"
)
```

## Step 4 — Verify

- `basic-memory:read_note(identifier="<USERNAME>/todo/main")` returns the note with the new bullet present under `## Active`.
- Report success: "Added: - [ ] <text>".
````

- [ ] **Step 2: Create `plugin/bm-scribe/skills/list-todos/SKILL.md`**

Use `Write`:

````markdown
---
name: bm-scribe:list-todos
description: Use when reviewing your personal rolling todo list. Prints all bullets from `<USERNAME>/todo/main` with numeric indices for use with `tick-todo`.
---

# list-todos

Reads `<USERNAME>/todo/main` and prints every checkbox bullet with a 1-based index.

## Step 1 — Resolve username

- `<USERNAME>` = `$BM_SCRIBE_USERNAME`, then `$USER` (POSIX). If both unset, ask the user for their handle and cache the answer for the session.

## Step 2 — Read the note

```text
basic-memory:read_note(identifier="<USERNAME>/todo/main")
```

If it errors with "not found", report "No todos yet. Use `bm-scribe:add-todo` to start." and stop.

## Step 3 — Parse and print

Walk the body line by line. For each line matching `^- \[[ x]\] (.+)$`:

- Maintain a 1-based counter `n`, incremented for every matching line (both checked and unchecked).
- Print: `n. [<state>] <text>` where `<state>` is `x` for checked, ` ` for unchecked.

Print the `## Active` and `## Done` section headers as section breaks above their bullets so the user can see structure.

## Step 4 — Report counts

After listing, print a summary line: `<active-count> active, <done-count> done`.
````

- [ ] **Step 3: Create `plugin/bm-scribe/skills/tick-todo/SKILL.md`**

Use `Write`:

````markdown
---
name: bm-scribe:tick-todo
description: Use when marking a personal todo as done. Flips the n'th unchecked bullet in `<USERNAME>/todo/main` from `- [ ]` to `- [x]` and appends a date stamp.
---

# tick-todo

Marks the n'th unchecked bullet in `<USERNAME>/todo/main` as done.

## Step 1 — Gather inputs

- `n` — 1-based index of the bullet to tick. Comes from the invocation argument. Indices come from `bm-scribe:list-todos` output.
- `<USERNAME>` = `$BM_SCRIBE_USERNAME`, then `$USER` (POSIX). If both unset, ask the user for their handle and cache the answer for the session.

## Step 2 — Find the target bullet

Read the note:

```text
basic-memory:read_note(identifier="<USERNAME>/todo/main")
```

Walk the body looking for lines matching `^- \[[ x]\] (.+)$`. Use the 1-based counter from `list-todos` (count BOTH checked and unchecked bullets). Locate the n'th bullet; capture its exact line text as `TARGET_LINE`.

If `TARGET_LINE` is already `- [x] …` (already done), report "Already done." and stop.

## Step 3 — Compute the replacement

`TARGET_LINE` looks like `- [ ] <text>`. The replacement is `- [x] <text> — done <YYYY-MM-DD>` where the date is today (UTC).

## Step 4 — Issue the edit

```text
basic-memory:edit_note(
  identifier="<USERNAME>/todo/main",
  operation="find_replace",
  find_text=<TARGET_LINE>,
  replace_text="- [x] <text> — done <YYYY-MM-DD>"
)
```

## Step 5 — Verify

- Re-read the note and confirm the bullet at index `n` is now `- [x]`.
- Report: "Ticked: <text> — done <YYYY-MM-DD>".
````

- [ ] **Step 4: Verify all three files exist with frontmatter**

```bash
ls plugin/bm-scribe/skills/{add-todo,list-todos,tick-todo}/SKILL.md
for f in plugin/bm-scribe/skills/{add-todo,list-todos,tick-todo}/SKILL.md; do
  head -5 "$f" | grep -E '^name:|^description:' | wc -l
done
```

Expected: three paths; each grep returns `2`.

- [ ] **Step 5: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Added` under `## [0.7.1]`:

```markdown
- Three personal-namespace todo skills under `plugin/bm-scribe/skills/`: `add-todo` (handles both create-on-first-use via the three-step pattern and subsequent appends via `insert_before_section`), `list-todos` (prints bullets with numeric indices), `tick-todo` (flips an unchecked bullet to checked with date stamp via `find_replace`). All three target `<USERNAME>/todo/main`.
```

- [ ] **Step 6: Commit**

```bash
git add plugin/bm-scribe/skills/{add-todo,list-todos,tick-todo} CHANGELOG.md
git commit -m "feat(plugin): add personal-namespace todo skills"
```

---

### Task 6: Implement three personal-namespace note skills

**Files:**
- Create: `plugin/bm-scribe/skills/add-note/SKILL.md`
- Create: `plugin/bm-scribe/skills/fetch-note/SKILL.md`
- Create: `plugin/bm-scribe/skills/list-notes/SKILL.md`
- Modify: `CHANGELOG.md` (append bullet).

**Goal:** Three skills that own per-topic personal notes under `<USERNAME>/notes/<slug>/main`.

**Acceptance criteria:**
- All three SKILL.md files exist with valid YAML frontmatter.
- All three skills resolve `<USERNAME>` per the global rule (`$BM_SCRIBE_USERNAME` → `$USER` → ask the user) and substitute the resolved handle into every `basic-memory` call. The literal string `<USERNAME>` is never sent to BM.
- `add-note` does the full three-step create. The note body is the user-supplied free-form markdown from Step 1 (no template required); frontmatter includes `title`, `permalink: <resolved-username>/notes/<slug>/main`, `note_type: personal_note`, and optional `tags`.
- `fetch-note` calls `read_note` with the canonical permalink. Output shape: print `title: <value>` and `tags: <value>` from the frontmatter (other frontmatter fields suppressed; if `tags` is missing or empty, print `tags: []`); blank line; then the full body verbatim.
- `list-notes` uses `search_notes` with a permalink-prefix filter (`<USERNAME>/notes/`) and prints `<title> — [[<permalink>]]` per result, sorted by title; reports `<N> personal note(s).` summary. Empty result reports "No personal notes yet. Use `bm-scribe:add-note <slug>` to create one." and exits.
- CHANGELOG bullet added.

**Non-goals:**
- Editing existing notes (out of scope for v0.7.1).
- Deleting notes (out of scope for v0.7.1).

- [ ] **Step 1: Create `plugin/bm-scribe/skills/add-note/SKILL.md`**

Use `Write`:

````markdown
---
name: bm-scribe:add-note
description: Use when creating a personal note on a topic. Creates `<USERNAME>/notes/<slug>/main` via the three-step pattern.
---

# add-note

Creates a personal note at `<USERNAME>/notes/<slug>/main`.

## Step 1 — Gather inputs

- `slug` — kebab-case identifier for the note. Comes from the invocation argument.
- `title` — one-line human title.
- `body` — free-form markdown.
- `tags` — optional list of tags.
- `<USERNAME>` = `$BM_SCRIBE_USERNAME`, then `$USER` (POSIX). If both unset, ask the user for their handle and cache the answer for the session.

## Step 2 — Resolve permalink

Canonical permalink = `<USERNAME>/notes/<slug>/main`. Directory = `<USERNAME>/notes/<slug>`.

## Step 3 — Issue the three-step BM call sequence

Follow [`../../docs/three-step-pattern.md`](../../docs/three-step-pattern.md):

```text
# 3a — write. BM ignores metadata.permalink.
basic-memory:write_note(
  title=<title>,
  directory="<USERNAME>/notes/<slug>",
  note_type="personal_note",
  content=<rendered body>,
  metadata={
    permalink: "<USERNAME>/notes/<slug>/main",
    tags: <tags or []>
  }
)
# Capture AUTO_PERMALINK.

# 3b — move.
basic-memory:move_note(
  identifier=AUTO_PERMALINK,
  destination_path="<USERNAME>/notes/<slug>/main.md"
)

# 3c — read to find the current YAML permalink line.
basic-memory:read_note(identifier="<USERNAME>/notes/<slug>/main")
# Extract `permalink: …` line.

# 3d — rewrite the permalink.
basic-memory:edit_note(
  identifier="<USERNAME>/notes/<slug>/main",
  operation="find_replace",
  find_text=<current permalink line>,
  replace_text="permalink: <USERNAME>/notes/<slug>/main"
)
```

## Step 4 — Verify

- `basic-memory:read_note(identifier="<USERNAME>/notes/<slug>/main")` returns the note.
- YAML `permalink:` equals `<USERNAME>/notes/<slug>/main` exactly.
- Report: "Created note: [[<USERNAME>/notes/<slug>/main]]".
````

- [ ] **Step 2: Create `plugin/bm-scribe/skills/fetch-note/SKILL.md`**

Use `Write`:

````markdown
---
name: bm-scribe:fetch-note
description: Use when reading a personal note. Reads `<USERNAME>/notes/<slug>/main` and prints it.
---

# fetch-note

Reads and prints a personal note at `<USERNAME>/notes/<slug>/main`.

## Step 1 — Gather inputs

- `slug` — kebab-case identifier. Comes from the invocation argument.
- `<USERNAME>` = `$BM_SCRIBE_USERNAME`, then `$USER` (POSIX). If both unset, ask the user for their handle and cache the answer for the session.

## Step 2 — Read

```text
basic-memory:read_note(identifier="<USERNAME>/notes/<slug>/main")
```

If it errors with "not found", report "No note at `<USERNAME>/notes/<slug>/main`. Did you mean to `bm-scribe:add-note <slug>`?" and stop.

## Step 3 — Print

Print the note in this exact shape: line 1 is `title: <value>` from the frontmatter; line 2 is `tags: <value>` from the frontmatter (if `tags` is missing or the list is empty, print `tags: []`); line 3 is blank; from line 4 onward, the full body verbatim. All other frontmatter fields (including `permalink`, `note_type`) are suppressed. Preserve markdown formatting in the body.
````

- [ ] **Step 3: Create `plugin/bm-scribe/skills/list-notes/SKILL.md`**

Use `Write`:

````markdown
---
name: bm-scribe:list-notes
description: Use when listing all your personal notes. Prints titles + permalinks for every note under `<USERNAME>/notes/`.
---

# list-notes

Lists all personal notes under `<USERNAME>/notes/`.

## Step 1 — Resolve username

- `<USERNAME>` = `$BM_SCRIBE_USERNAME`, then `$USER` (POSIX). If both unset, ask the user for their handle and cache the answer for the session.

## Step 2 — Search

```text
basic-memory:search_notes(
  query="",
  filters={ permalink_prefix: "<USERNAME>/notes/" }
)
```

(If `basic-memory:search_notes` uses a different filter shape in your installed version, fall back to `query="<USERNAME>/notes/"` — BM's full-text search will match the prefix in the permalink.)

## Step 3 — Print

For each result, print one line: `<title> — [[<permalink>]]`. Sort by title.

If the result set is empty, report "No personal notes yet. Use `bm-scribe:add-note <slug>` to create one." and stop.

## Step 4 — Summary

After listing, print: `<N> personal note(s).`
````

- [ ] **Step 4: Verify all three files**

```bash
ls plugin/bm-scribe/skills/{add-note,fetch-note,list-notes}/SKILL.md
for f in plugin/bm-scribe/skills/{add-note,fetch-note,list-notes}/SKILL.md; do
  head -5 "$f" | grep -E '^name:|^description:' | wc -l
done
```

Expected: three paths; each grep returns `2`.

- [ ] **Step 5: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Added` under `## [0.7.1]`:

```markdown
- Three personal-namespace note skills under `plugin/bm-scribe/skills/`: `add-note` (three-step create at `<USERNAME>/notes/<slug>/main`), `fetch-note` (read + print), `list-notes` (search by `<USERNAME>/notes/` prefix and print titles + permalinks).
```

- [ ] **Step 6: Commit**

```bash
git add plugin/bm-scribe/skills/{add-note,fetch-note,list-notes} CHANGELOG.md
git commit -m "feat(plugin): add personal-namespace note skills"
```

---

### Task 7: Personal-namespace markdown templates

**Files:**
- Create: `examples/project-knowledge/personal/README.md`
- Create: `examples/project-knowledge/personal/todo.md`
- Create: `examples/project-knowledge/personal/note.md`
- Modify: `CHANGELOG.md` (append bullet).

**Goal:** Ship templates that adopters can copy verbatim into BM for their personal namespace. The `add-todo` skill specifically references `todo.md` as the body it instantiates on first-use create.

**Acceptance criteria:**
- All three files exist under `examples/project-knowledge/personal/`.
- `README.md` (≤ 40 lines) covers what the personal namespace is, scope, why anti-tangent ignores it, pointer to `plugin/bm-scribe/`.
- `todo.md` is a copy-pastable template with frontmatter (`title`, `permalink`, `note_type: personal_todo`) and two body sections (`## Active`, `## Done`).
- `note.md` is a copy-pastable template with frontmatter (`title`, `permalink`, `note_type: personal_note`, `tags`) and a one-section body skeleton.
- CHANGELOG `### Added` bullet appended.

- [ ] **Step 1: Create `examples/project-knowledge/personal/README.md`**

Use `Write`:

````markdown
# Personal namespace templates

Templates for the per-user personal namespace introduced in v0.7.1. The `plugin/bm-scribe/` plugin in this repo writes notes that conform to these shapes; humans can also paste them directly into BM.

## What the personal namespace is

The personal namespace lives **in the same BM project** as project-knowledge notes, namespaced by the user's handle (e.g. `pgilmore`, `alice`, `bob`). Two note types ship in v0.7.1:

- `<USERNAME>/todo/main.md` — one rolling checkbox list per user.
- `<USERNAME>/notes/<slug>/main.md` — one personal note per topic.

## Why anti-tangent ignores it

`prime_project_knowledge` and `extract_project_knowledge` operate over the `<PROJECT>/` prefix only — personal-namespace permalinks (`<USERNAME>/…`) are never indexed for the reviewer, never picked for primes, never proposed by extract. This is enforced at the `kb_index` filter the controller passes in; anti-tangent itself never sees the `<USERNAME>/` notes.

The boundary is intentional: personal notes are personal. Project knowledge is shared.

## See also

- [`docs/team-setup/project-knowledge-conventions.md`](../../../docs/team-setup/project-knowledge-conventions.md) § 9 — full conventions for the personal namespace.
- [BM scribe design spec](../../../docs/superpowers/specs/2026-05-21-bm-scribe-design.md) — the plugin that writes these.
- [`plugin/bm-scribe/`](../../../plugin/bm-scribe/) — the plugin implementation.
````

- [ ] **Step 2: Create `examples/project-knowledge/personal/todo.md`**

Use `Write`:

````markdown
---
title: <USERNAME>'s todo
permalink: <USERNAME>/todo/main
note_type: personal_todo
---

# Todo

One rolling checkbox list for personal todos. Edited via `bm-scribe:add-todo` / `bm-scribe:tick-todo` / `bm-scribe:list-todos`, or directly in any markdown editor.

## Active

- [ ] First todo item
- [ ] Second todo item

## Done

- [x] Example completed item — done 2026-05-21
````

- [ ] **Step 3: Create `examples/project-knowledge/personal/note.md`**

Use `Write`:

````markdown
---
title: <Note title>
permalink: <USERNAME>/notes/<slug>/main
note_type: personal_note
tags: []
---

# <Note title>

One note per topic. Free-form body. Created via `bm-scribe:add-note <slug>`, read via `bm-scribe:fetch-note <slug>`.

## Body

Free-form content here. Markdown supported. Cross-links to project knowledge are allowed (e.g. `[[<PROJECT>/decisions/0001-text-only-reviewer/main]]`), but the personal-namespace note is never picked up by anti-tangent's prime / extract.
````

- [ ] **Step 4: Verify**

```bash
ls examples/project-knowledge/personal/
```

Expected: `README.md`, `note.md`, `todo.md`.

- [ ] **Step 5: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Added` under `## [0.7.1]`:

```markdown
- New personal-namespace templates under `examples/project-knowledge/personal/`: `README.md` (overview), `todo.md` (rolling checkbox list at `<USERNAME>/todo/main` with `## Active` / `## Done` sections), and `note.md` (one note per topic at `<USERNAME>/notes/<slug>/main`). The `bm-scribe:add-todo` skill instantiates `todo.md` on first-use create.
```

- [ ] **Step 6: Commit**

```bash
git add examples/project-knowledge/personal/ CHANGELOG.md
git commit -m "docs(examples): add personal-namespace templates (v0.7.1)"
```

---

### Task 8: Add §9 "Personal namespace" to the conventions doc

**Files:**
- Modify: `docs/team-setup/project-knowledge-conventions.md` (insert §9 between §8 and "See also").
- Modify: `CHANGELOG.md` (append bullet under `### Added`).

**Goal:** Document the personal namespace in the same place adopters look for everything else (folder convention, issue-ID format, milestone events).

**Acceptance criteria:**
- `docs/team-setup/project-knowledge-conventions.md` has a new `## 9. Personal namespace (\`<USERNAME>/\`)` section sitting between `## 8. Maintenance ownership` and the `---` separator before "See also". Uses `<USERNAME>` as the placeholder throughout (consistent with the rest of this plan, `plugin/bm-scribe/`, and the personal-namespace templates).
- §9 covers: same BM project as project-knowledge, namespaced by user handle; `<USERNAME>/todo/main.md` (rolling list) and `<USERNAME>/notes/<slug>/main.md` (per-topic) layouts; explicit statement that extract / prime do NOT scan the personal namespace; pointer to `plugin/bm-scribe/` for the write side.
- The "See also" block at the bottom gains a bullet pointing at `examples/project-knowledge/personal/` and one at `plugin/bm-scribe/`.
- CHANGELOG bullet added.

- [ ] **Step 1: Insert §9 into the conventions doc**

Use `Edit` on `docs/team-setup/project-knowledge-conventions.md`. Find:

```markdown
The principle: **extract proposes dashboard updates from milestone events; humans curate the durable layer**.

---

## See also
```

Replace with:

```markdown
The principle: **extract proposes dashboard updates from milestone events; humans curate the durable layer**.

## 9. Personal namespace (`<USERNAME>/`)

v0.7.1 introduces an optional personal namespace that lives **inside the same BM project** as project knowledge, namespaced by each user's handle:

- `<USERNAME>/todo/main.md` — one rolling personal todo list per user (markdown checkbox bullets).
- `<USERNAME>/notes/<slug>/main.md` — one personal note per topic.

The personal namespace is **invisible to anti-tangent**. `prime_project_knowledge` and `extract_project_knowledge` operate over the `<PROJECT>/` prefix only; controllers should pass a `kb_index` that excludes `<USERNAME>/*` permalinks before calling either tool. This is the boundary that lets personal notes coexist with project knowledge in one BM project without leaking into the reviewer's context.

The `plugin/bm-scribe/` plugin owns the write side (`bm-scribe:add-todo`, `bm-scribe:add-note`, etc.); see [BM scribe design spec](../superpowers/specs/2026-05-21-bm-scribe-design.md) for the contract and [`examples/project-knowledge/personal/`](../../examples/project-knowledge/personal/) for ready-to-paste templates.

`<USERNAME>` is the user's chosen handle, not necessarily their OS username. One value per Claude Code installation; the plugin caches it in local state.

---

## See also
```

- [ ] **Step 2: Add new pointers to "See also"**

Edit the same file. Find:

```markdown
- [`examples/project-knowledge/`](../../examples/project-knowledge/) — the shipped templates.
- [`examples/project-knowledge/dogfood/`](../../examples/project-knowledge/dogfood/) — frozen-snapshot real anti-tangent example notes.
```

Replace with:

```markdown
- [`examples/project-knowledge/`](../../examples/project-knowledge/) — the shipped templates.
- [`examples/project-knowledge/dogfood/`](../../examples/project-knowledge/dogfood/) — frozen-snapshot real anti-tangent example notes.
- [`examples/project-knowledge/personal/`](../../examples/project-knowledge/personal/) — personal-namespace templates (v0.7.1).
- [`plugin/bm-scribe/`](../../plugin/bm-scribe/) — the Claude Code plugin that writes notes per these conventions (v0.7.1).
```

- [ ] **Step 3: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Added` under `## [0.7.1]`:

```markdown
- New §9 "Personal namespace (`<USERNAME>/`)" in `docs/team-setup/project-knowledge-conventions.md` documenting the `<USERNAME>/todo/main` and `<USERNAME>/notes/<slug>/main` layouts, the same-BM-project posture, the explicit boundary that anti-tangent's `prime` / `extract` never scan the personal namespace, and a pointer to `plugin/bm-scribe/` for the write side.
```

- [ ] **Step 4: Verify section ordering**

```bash
grep -n '^## ' docs/team-setup/project-knowledge-conventions.md
```

Expected: `## 1.` through `## 9.` appear in order, followed by `## See also`.

- [ ] **Step 5: Commit**

```bash
git add docs/team-setup/project-knowledge-conventions.md CHANGELOG.md
git commit -m "docs(conventions): document personal namespace in §9 (v0.7.1)"
```

---

### Task 9: Reposition BM v0.21.1 subsection + add 3-step worked example in INTEGRATION.md

**Files:**
- Modify: `INTEGRATION.md`
- Modify: `CHANGELOG.md` (append bullet under `### Changed`).

**Goal:** Move the "Applying bm_commands to BM v0.21.1" subsection so it's read **before** the bm_commands paste step, and add an end-to-end worked example showing `write_note → move_note → edit_note` with annotated outputs. Mention the new `plugin/bm-scribe/` as the encoded form of this contract.

**Acceptance criteria:**
- The entire "### Applying bm_commands to BM v0.21.1" subsection moves from its current position (between "Environment variables" and §5) to sit directly under "### Controller workflow (per epic)", before "### Six note types in two layers".
- The relocated subsection gains a "**Worked example — landing one epic note end-to-end**" block with literal BM call shapes and annotated `# →` comments.
- The relocated subsection mentions `plugin/bm-scribe/` as the encoded form of the three-step pattern.
- All cross-references to the subsection elsewhere in the repo (search for "BM v0.21.1") continue to resolve — i.e. the subsection heading text is preserved verbatim.
- CHANGELOG bullet added under `### Changed`.

- [ ] **Step 1: Cut the BM v0.21.1 subsection from its current position**

Use `Edit` on `INTEGRATION.md`. Find the entire existing subsection — from `### Applying bm_commands to BM v0.21.1` through the last paragraph ending `…keeping cross-links load-bearing across calls.` (inclusive of the trailing blank line). Replace with the empty string `` to delete.

Use this verbatim block as the `old_string`:

```markdown
### Applying bm_commands to BM v0.21.1

Anti-tangent's `bm_commands` arrays are paste-ready *conceptual* shape — the tool names match BM verbatim, but the arg shapes track the spec's logical model rather than each BM release's literal signature (the explicit non-goal: don't couple anti-tangent to BM's per-release API churn). Field-tested against BM v0.21.1 on 2026-05-21, three small translation steps land between paste and apply.

**`write_note` arg mapping** (extract's `Proposal{action: "create"}` and supersede-leg-1):

| Extract emits | BM v0.21.1 takes | Mapping |
|---|---|---|
| `permalink: "<dir>/<slug>"` | `directory` + `title` | Split on the last `/`; prefix is `directory`. Pass `proposal.title` directly as `title` rather than slug-back-to-title. |
| `frontmatter: {…}` | `metadata: {…}` | Verbatim — BM merges into the YAML frontmatter at the top of the file. |
| `body: "…"` | `content: "…"` | Verbatim. |
| `proposal.type` | `note_type` | E.g. `"decision"`, `"epic"`, `"feature"`, `"module"`, `"glossary"`. |

**`edit_note` operation hints.** BM v0.21.1's `edit_note` requires an explicit `operation` enum that extract does not emit; the agent picks based on the target note's structure:

- Ledger / "Recent material changes" appends — `insert_before_section` keyed on the section AFTER your target (puts the new entry at the bottom of the target section without clobbering).
- Supersede-leg-2 (flipping a predecessor's `status` to `superseded`) — `find_replace` against the frontmatter line, or BM's frontmatter-patch verb if available in your version.
- Replacing a whole section's body — `replace_section`.
- Appending to the very end of the note (no section anchor) — `append`.

**Permalink-slug expectations.** BM auto-derives the stored slug from the `title` (lowercased, hyphenated, no date prefix unless the title carries one), so the permalink proposed by extract (e.g. `decisions/2026-05-docker-bm-deployment-is-alternative`) often diverges from what BM stores (e.g. `main/decisions/docker-basic-memory-deployment-is-an-alternative-path`). If the same extract run proposes cross-links (e.g. an `epic_origin` ledger entry referencing the new note), they won't resolve against BM's actual slug. Cleanest fix: `move_note` after `write_note` to rename to the anti-tangent-proposed permalink, keeping cross-links load-bearing across calls.

```

Replace with empty string to delete.

- [ ] **Step 2: Insert the relocated subsection with the worked example after "Controller workflow (per epic)"**

Use `Edit` on `INTEGRATION.md`. Find:

```markdown
4. **Apply.** A human (or the controller, gated by the ladder below) reviews proposals and pastes the `bm_commands`.

### Six note types in two layers
```

Replace with:

````markdown
4. **Apply.** A human (or the controller, gated by the ladder below) reviews proposals and pastes the `bm_commands` — see the "Applying bm_commands to BM v0.21.1" subsection immediately below for the translation steps **before** you paste.

### Applying bm_commands to BM v0.21.1

Anti-tangent's `bm_commands` arrays are paste-ready *conceptual* shape — the tool names match BM verbatim, but the arg shapes track the spec's logical model rather than each BM release's literal signature (the explicit non-goal: don't couple anti-tangent to BM's per-release API churn). Field-tested against BM v0.21.1 on 2026-05-21, three small translation steps land between paste and apply.

**`write_note` arg mapping** (extract's `Proposal{action: "create"}` and supersede-leg-1):

| Extract emits | BM v0.21.1 takes | Mapping |
|---|---|---|
| `permalink: "<dir>/<slug>"` | `directory` + `title` | Split on the last `/`; prefix is `directory`. Pass `proposal.title` directly as `title` rather than slug-back-to-title. |
| `frontmatter: {…}` | `metadata: {…}` | Verbatim — BM merges into the YAML frontmatter at the top of the file. |
| `body: "…"` | `content: "…"` | Verbatim. |
| `proposal.type` | `note_type` | E.g. `"decision"`, `"epic"`, `"feature"`, `"module"`, `"glossary"`. |

**`edit_note` operation hints.** BM v0.21.1's `edit_note` requires an explicit `operation` enum that extract does not emit; the agent picks based on the target note's structure:

- Ledger / "Recent material changes" appends — `insert_before_section` keyed on the section AFTER your target (puts the new entry at the bottom of the target section without clobbering).
- Supersede-leg-2 (flipping a predecessor's `status` to `superseded`) — `find_replace` against the frontmatter line, or BM's frontmatter-patch verb if available in your version.
- Replacing a whole section's body — `replace_section`.
- Appending to the very end of the note (no section anchor) — `append`.

**Permalink-slug expectations.** BM auto-derives the stored slug from the `title` (lowercased, hyphenated, no date prefix unless the title carries one), so the permalink proposed by extract (e.g. `<PROJECT>/decisions/0042-docker-bm-deployment-is-alternative`) often diverges from what BM stores (e.g. `main/decisions/docker-basic-memory-deployment-is-an-alternative-path`). If the same extract run proposes cross-links (e.g. an `epic_origin` ledger entry referencing the new note), they won't resolve against BM's actual slug. Cleanest fix: a **three-step pattern** — `write_note` to create, `move_note` to relocate to the canonical path, `edit_note` to rewrite the YAML `permalink:` line so the in-frontmatter permalink matches the canonical path. Step 3 is the load-bearing one; steps 1+2 alone leave wikilinks broken.

**Worked example — landing one epic note end-to-end.**

```text
# Step 1 — create the note. BM ignores metadata.permalink and auto-derives.
write_note(
  title="YN-10206 — DriverInvite testability",
  directory="monorepo/epics/YN-10206",
  note_type="epic",
  content="<charter body>",
  metadata={ permalink: "monorepo/epics/YN-10206/main", … }
)
# → BM creates: main/monorepo/epics/yn-10206-driverinvite-testability.md
# → Frontmatter has: permalink: main/monorepo/epics/yn-10206-driverinvite-testability
# → NOT the canonical permalink we asked for.

# Step 2 — move the file to the canonical path.
move_note(
  identifier="main/monorepo/epics/yn-10206-driverinvite-testability",
  destination_path="monorepo/epics/YN-10206/main.md"
)
# → File now lives at: monorepo/epics/YN-10206/main.md
# → BUT the YAML frontmatter still says: permalink: main/monorepo/epics/yn-10206-driverinvite-testability
# → Wikilinks like [[monorepo/epics/YN-10206/main]] do NOT resolve yet.

# Step 3 — rewrite the YAML permalink line. This is the load-bearing step.
edit_note(
  identifier="monorepo/epics/YN-10206/main",
  operation="find_replace",
  find_text="permalink: main/monorepo/epics/yn-10206-driverinvite-testability",
  replace_text="permalink: monorepo/epics/YN-10206/main"
)
# → Frontmatter updated. Wikilinks [[monorepo/epics/YN-10206/main]] now resolve.
```

The `plugin/bm-scribe/` plugin shipped from this repo encodes this pattern across every creator skill so calling agents don't have to rediscover step 3 empirically. See [`plugin/bm-scribe/docs/three-step-pattern.md`](plugin/bm-scribe/docs/three-step-pattern.md) for the load-bearing reference.

### Six note types in two layers
````

- [ ] **Step 3: Verify the subsection moved cleanly**

```bash
grep -n '### Applying bm_commands to BM v0.21.1' INTEGRATION.md
grep -n '### Six note types in two layers' INTEGRATION.md
```

Expected: exactly one match for each. The BM v0.21.1 line number is **less than** the Six note types line number (i.e. it now sits above).

- [ ] **Step 4: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Changed` under `## [0.7.1]`:

```markdown
- `INTEGRATION.md` "Project knowledge (optional)" section: moved the "Applying bm_commands to BM v0.21.1" subsection up so it sits directly under "Controller workflow (per epic)" — readers now see the translation contract **before** any bm_commands paste step. Added an end-to-end worked example showing `write_note → move_note → edit_note` with annotated outputs so the three-step permalink-canonicalization pattern is concrete, not implicit. Subsection points at `plugin/bm-scribe/` as the encoded form of the contract. Prior 0.6.2 file-size constraint (40k user-instructions threshold) is dropped — INTEGRATION.md grows as needed.
```

- [ ] **Step 5: Commit**

```bash
git add INTEGRATION.md CHANGELOG.md
git commit -m "docs(integration): move BM v0.21.1 quirks up + add 3-step worked example"
```

---

### Task 10: Add v0.7.0 canonical-layout callout in INTEGRATION.md

**Files:**
- Modify: `INTEGRATION.md`
- Modify: `CHANGELOG.md` (append bullet under `### Changed`).

**Goal:** Surface the v0.7.0 canonical layout (`<PROJECT>/<type>/<key>/main.md`, plural type folders, ADR-numbered decisions) inline so agents don't have to crawl into the conventions doc.

**Acceptance criteria:**
- `INTEGRATION.md` gains a new `### v0.7.0 canonical layout` subsection between "### Six note types in two layers" and "### The `project_knowledge` field".
- Callout states: plural type folders (`epics/`, `stories/`, `decisions/`, `modules/`, `features/`, `glossary/`); `main.md` literal filename; decisions use `NNNN-<slug>` (ADR-numbered) not date-prefix; v0.6.x flat shape is legacy.
- Includes a concrete example permalink per type.
- Mentions `plugin/bm-scribe/` as the canonical writer.
- CHANGELOG bullet added.

- [ ] **Step 1: Insert the v0.7.0 canonical-layout subsection**

Use `Edit` on `INTEGRATION.md`. Find:

```markdown
Templates live in [`examples/project-knowledge/`](examples/project-knowledge/); frozen-snapshot real anti-tangent examples in [`examples/project-knowledge/dogfood/`](examples/project-knowledge/dogfood/). For the per-project tuning loop — issue-ID format, folder convention, milestone events, project-prefix bootstrap (including v0.6.x→v0.7.0 migration) — see [`docs/team-setup/project-knowledge-conventions.md`](docs/team-setup/project-knowledge-conventions.md).

### The `project_knowledge` field
```

Replace with:

````markdown
Templates live in [`examples/project-knowledge/`](examples/project-knowledge/); frozen-snapshot real anti-tangent examples in [`examples/project-knowledge/dogfood/`](examples/project-knowledge/dogfood/). For the per-project tuning loop — issue-ID format, folder convention, milestone events, project-prefix bootstrap (including v0.6.x→v0.7.0 migration) — see [`docs/team-setup/project-knowledge-conventions.md`](docs/team-setup/project-knowledge-conventions.md).

### v0.7.0 canonical layout

All note permalinks follow `<PROJECT>/<type>/<key>/main`, with `main.md` as a literal filename (leaves room for `charter.md`, `retro.md`, etc. as side-docs per ticket):

| Type | Permalink shape | Example |
|---|---|---|
| `epic` | `<PROJECT>/epics/<TICKET-ID>/main` | `monorepo/epics/YN-10206/main` |
| `story` | `<PROJECT>/stories/<TICKET-ID>/main` | `monorepo/stories/YN-10211/main` |
| `decision` | `<PROJECT>/decisions/<NNNN>-<slug>/main` | `monorepo/decisions/0001-text-only-reviewer/main` |
| `module` | `<PROJECT>/modules/<slug>/main` | `monorepo/modules/review-pipeline/main` |
| `feature` | `<PROJECT>/features/<slug>/main` | `monorepo/features/project-knowledge-loop/main` |
| `glossary` | `<PROJECT>/glossary/<term>/main` | `monorepo/glossary/dispatch-clause/main` |

Type folders are **plural**. Decisions are **ADR-numbered** (`NNNN-<slug>`), not date-prefixed — date-prefix forms (`2026-05-<slug>`) are a v0.6.x artifact and should be migrated to ADR shape on first edit (see conventions doc § 6, Path A). The `plugin/bm-scribe/` plugin (shipped from this repo as of v0.7.1) auto-picks the next ADR number when creating a decision and enforces this layout for every other type.

### The `project_knowledge` field
````

- [ ] **Step 2: Verify ordering**

```bash
grep -n -E '^### (Six note types in two layers|v0\.7\.0 canonical layout|The `project_knowledge` field)' INTEGRATION.md
```

Expected: three matches in this order — Six note types, v0.7.0 canonical layout, The `project_knowledge` field.

- [ ] **Step 3: Append CHANGELOG bullet**

Edit `CHANGELOG.md`. Append to `### Changed` under `## [0.7.1]`:

```markdown
- `INTEGRATION.md` gains a new "v0.7.0 canonical layout" subsection inline (between "Six note types in two layers" and "The `project_knowledge` field"). Tabulates the canonical permalink shape per note type with concrete examples, calls out plural type folders, ADR-numbered decisions (not date-prefix), and the legacy posture of v0.6.x flat shapes. References `plugin/bm-scribe/` as the canonical writer.
```

- [ ] **Step 4: Commit**

```bash
git add INTEGRATION.md CHANGELOG.md
git commit -m "docs(integration): add v0.7.0 canonical-layout callout"
```

---

### Task 11: Reconcile date-prefix examples + bump VERSION + release-ready commit

**Files:**
- Modify: `INTEGRATION.md` (any remaining date-prefix decision examples → ADR shape).
- Modify: `VERSION` (bump `0.7.0` → `0.7.1`).
- Modify: `CHANGELOG.md` (date sanity check, optional final bullet).

**Goal:** Finish with no stale date-prefix decision references, bump VERSION, and land the release-ready commit. Analog of `d2d0490 chore(release): mark v0.7.0 release-ready`.

**Acceptance criteria:**
- `grep -nE 'decisions/[0-9]{4}-[0-9]{2}-' INTEGRATION.md` returns zero matches.
- `VERSION` contains exactly `0.7.1` (single line, single trailing newline).
- CHANGELOG `## [0.7.1] - 2026-05-21` date is current; if work has spanned multiple days, update to today's date.
- Final commit subject: `chore(release): mark v0.7.1 release-ready`.

**Non-goals:**
- Pushing to origin or opening / promoting the PR — those happen at the workflow layer, outside this plan.

- [ ] **Step 1: Sweep for stale date-prefix decision references in INTEGRATION.md**

```bash
grep -nE 'decisions/[0-9]{4}-[0-9]{2}-' INTEGRATION.md
```

If matches exist, use `Edit` to replace each with the ADR-shaped form. The canonical replacement for the field-tested Docker BM example is `<PROJECT>/decisions/0042-docker-bm-deployment-is-alternative`. Task 9 already updated one instance; this is the catch-all sweep.

- [ ] **Step 2: Bump VERSION**

Use `Edit` on `VERSION`. Find:

```
0.7.0
```

Replace with:

```
0.7.1
```

- [ ] **Step 3: Confirm CHANGELOG date is current**

```bash
date -I
```

If today is `2026-05-21`, the CHANGELOG date is already correct — skip the next edit. Otherwise, edit `## [0.7.1] - 2026-05-21` to `## [0.7.1] - <today>`.

- [ ] **Step 4: Final verification — read back the full CHANGELOG entry**

```bash
sed -n '/^## \[0\.7\.1\]/,/^## \[0\.7\.0\]/p' CHANGELOG.md
```

Expected: at least seven bullets under `### Added` (from Tasks 2-8) and at least two under `### Changed` (from Tasks 9-10). Other subsections present but empty.

- [ ] **Step 5: Final commit**

```bash
git add INTEGRATION.md CHANGELOG.md VERSION
git commit -m "chore(release): mark v0.7.1 release-ready"
```

- [ ] **Step 6: Sanity-check the branch is ready for PR**

```bash
git log --reverse --oneline main..HEAD
```

Expected: the eleven planned commit subjects (from Tasks 1-11) appear in plan order among the branch's commits (oldest first, courtesy of `--reverse`). Pre-existing commits made before Task 1 (e.g. the plan-landing commit `docs(plan): add v0.7.1 implementation plan` and any CR-review-loop commits) may appear at the top of the log — that is expected, not a failure. What matters: scanning the output top-to-bottom, the eleven planned subjects (starting with `chore(release): start v0.7.1 work` for Task 1 and ending with `chore(release): mark v0.7.1 release-ready` for Task 11) must each appear in order, with no planned subject missing. If a planned subject is missing, the corresponding task did not commit and must be re-run.

---

## Post-plan checklist (workflow-side, after all tasks land)

These are NOT plan tasks — they happen at the workflow layer that wraps this plan's execution:

- Push `version/0.7.1` to origin (`git push -u origin version/0.7.1`).
- The draft PR opened earlier in the workflow is already live; promote it to ready (`gh pr ready <number>`) after all tasks land.
- Final CodeRabbit review pass on the implementation diff.
- Close [#33](https://github.com/patiently/anti-tangent-mcp/issues/33) on PR merge.
- Mirror INTEGRATION.md updates into `~/.claude/anti-tangent.md` per the memory note "anti-tangent-mirror-integration-md" (downstream mirror).
